export interface Task {
    path: string
    file: string
}

export interface Location {
    date: Date
    dateTime: string
    time: string
    latitude: number
    longitude: number
    altitude: number
    bearing: string
    speed: string
}