export interface Task {
    path: string
    file: string
}

export interface Location {
    date: Date
    dateTime: string
    time: string
    latitude: string
    longitude: string
    altitude: string
    bearing: string
    speed: string
}